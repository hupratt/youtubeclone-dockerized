volumes:
  pg_data_youtubeclone:

networks:
  youtube_network:
    driver: bridge
    ipam:
     driver: default
     config:
       - subnet: 172.155.0.0/16
    driver_opts:
      com.docker.network.bridge.name: br_youtube_clone

services:
  backend:
    container_name: youtubeclone-backend
    build: .
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./youtubeclone-backend:/backend
      - ./youtubeclone-frontend:/frontend
      - ./static:/backend/static
    networks:
      - youtube_network
    env_file: 
      - .env
    # environment:
    #   DEBUG: 'true'

  postgres:
    restart: always
    image: postgres:12
    env_file:
      - .env
    ports:
      - "5432:5432"
    networks:
      - youtube_network
    volumes:
      - pg_data_youtubeclone:/var/lib/postgresql/data/
      - ./backups:/var/backups